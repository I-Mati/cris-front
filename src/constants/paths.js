const HOME = "/";
const REGISTRATION = "/registration";
const LEADS = "/leads";
const LEAD = `/lead/:id`;
const SUCCESS = "/success";

const NOT_FOUND = "*";

export { HOME, REGISTRATION, LEADS, LEAD, SUCCESS, NOT_FOUND };
